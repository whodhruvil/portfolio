[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "12.16.3"
  NPM_VERSION = "6.14.4"

# Redirects for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
